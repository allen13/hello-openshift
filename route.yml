apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app:
  name:
spec:
  port:
    targetPort: web
  to:
    kind: 
    name:
    weight: 100
  tls:
    termination:
  wildcardPolicy: None
